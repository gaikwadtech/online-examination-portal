# 🤝 Contributing to TestEdge

We welcome contributions to the TestEdge Examination Portal! By contributing, you agree to abide by our Code of Conduct.

## 🐛 Found a Bug?

1. Search the existing issues to see if the bug has already been reported.
2. If not, open a new issue. Clearly describe the bug, including steps to reproduce it, the expected behavior, and the actual behavior.

## ✨ Want a New Feature?

1. Before starting work, open an issue to discuss the new feature idea with the TestEdge team.
2. Explain the use case and why the feature would be valuable.

## 💻 Submitting Code (Pull Requests)

1. **Fork** the repository and clone it locally.
2. Create a new branch: `git checkout -b feature/your-feature-name` or `git checkout -b fix/issue-number`.
3. Make your changes, adhering to the project's coding style (e.g., standard TypeScript/React conventions).
4. Run `npm install` and `npm run dev` to ensure the project still runs locally.
5. Commit your changes with a clear, descriptive message (e.g., `feat: Implement question timer for exams`).
6. Push your branch and open a Pull Request (PR) against the `main` branch.

**In your PR, please:**
* Reference the related issue number (e.g., `Closes #42`).
* Provide a brief explanation of the changes.
